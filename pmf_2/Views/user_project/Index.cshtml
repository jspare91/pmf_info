@model IEnumerable<pmf_2.Models.user_project>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AspNetUser.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.project.project_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUser.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.project.project_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.user_proj_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.user_proj_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.user_proj_Id })
        </td>
    </tr>
}

</table>
